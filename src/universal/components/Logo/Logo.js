// Libraries
import React, {Component, PropTypes} from 'react';

import {
  logo
} from './logo.less';

class Logo extends Component {
  render () {
    return (
      <i className={logo}></i>
    );
  }
}

export default Logo;
